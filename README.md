# Parcel example package for Google Apps Script projects

This is a template package for Apps Script projects which uses `parcel` bundler.

Just copy `package.json` to your newly created project.

## LICENSE

CC0.